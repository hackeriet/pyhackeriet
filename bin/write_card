#!/usr/bin/env python

import uuid, sys, hashlib
from hackeriet import mifare

d = uuid.uuid4().bytes

printf("Waiting for card")
mifare.wait_card()
printf("Writing card ...")
mifare.write_sector(d, 7)

print("Hash: " + hashlib.sha256(d.bytes).hexdigest())
print("Wrote: " + str(d))
print("Read: " + str(mifare.read_sector(7)[0:16]))

